# Code that runs on the RaspberryPi 
## Classes
# Sensors.py
Contains a base class called Sensors and two derived classes, SpectralSensor and SoilSensor. Each Sensor object contains:
- `reading_name`: used to identify the sensor type
- ``
